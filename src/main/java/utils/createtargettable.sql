CREATE TABLE `target` (
        `id` int(11) NOT NULL AUTO_INCREMENT,
        `productname` varchar(20) ,
        `price` varchar(20),
        `url` VARCHAR (20),
        `imagename` VARCHAR (20),
        `locations` VARCHAR (20),
        PRIMARY KEY (`id`)
        );