CREATE TABLE `target` (
        `id` int(11) NOT NULL AUTO_INCREMENT,
        `productname` varchar(200) ,
        `price` varchar(20),
        `url` VARCHAR (200),
        `imagename` VARCHAR (200),
        `locations` VARCHAR (20),
        PRIMARY KEY (`id`)
        );