CREATE TABLE `users` (
        `id` int(11) NOT NULL AUTO_INCREMENT,
        `username` varchar(20) ,
        `password` varchar(20),
        `telephone` varchar(20),
        `email`  varchar(30),
        PRIMARY KEY (`id`)
        );